---
// Navbar Component
---

<nav class="navbar">
	<div class="container">
		<div class="nav-content">
			<a href="/" class="nav-brand">Cogni<span class="highlight">Core</span></a>
			<ul class="nav-links">
				<li><a href="#servicios">Servicios</a></li>
				<li><a href="#clientes">Clientes</a></li>
				<li><a href="#nosotros">Nosotros</a></li>
				<li><a href="#colaboradores">Colaboradores</a></li>
				<li><a href="#contacto" class="nav-cta">Contacto</a></li>
			</ul>
		</div>
	</div>
</nav>

<style>
	.navbar {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		background: rgba(255, 255, 255, 0.95);
		backdrop-filter: blur(10px);
		z-index: 1000;
		border-bottom: 1px solid rgba(6, 182, 212, 0.1);
		transition: all 0.3s ease;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 2rem;
	}

	.nav-content {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 1rem 0;
	}

	.nav-brand {
		font-size: 1.5rem;
		font-weight: 700;
		text-decoration: none;
		color: var(--dark);
	}

	.highlight {
		color: var(--turquoise);
	}

	.nav-links {
		display: flex;
		list-style: none;
		gap: 2rem;
		align-items: center;
	}

	.nav-links a {
		text-decoration: none;
		color: var(--gray);
		font-weight: 500;
		transition: color 0.3s ease;
	}

	.nav-links a:hover {
		color: var(--turquoise);
	}

	.nav-cta {
		padding: 0.5rem 1.5rem;
		background: var(--turquoise);
		color: white !important;
		border-radius: 6px;
	}

	.nav-cta:hover {
		background: var(--turquoise-dark);
	}

	@media (max-width: 768px) {
		.nav-links {
			display: none;
		}
	}
</style>
