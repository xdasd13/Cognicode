---
import Layout from '../../layouts/Layout.astro';
---

<Layout>
    <section class="contacto-wrapper">
        <header class="contacto-hero">
            <div class="hero-wave" aria-hidden="true">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 320" preserveAspectRatio="none">
                    <path
                        fill="#0099ff"
                        fill-opacity="1"
                        d="M0,64L48,90.7C96,117,192,171,288,165.3C384,160,480,96,576,80C672,64,768,96,864,122.7C960,149,1056,171,1152,160C1248,149,1344,107,1392,85.3L1440,64L1440,0L1392,0C1344,0,1248,0,1152,0C1056,0,960,0,864,0C768,0,672,0,576,0C480,0,384,0,288,0C192,0,96,0,48,0L0,0Z"
                    />
                </svg>
            </div>
            <div class="hero-card">
                <p class="hero-pill">Conversemos</p>
                <h1>Agenda tu proyecto con nuestro equipo</h1>
                <p class="hero-copy">
                    Cuéntanos qué necesitas construir y diseñaremos un plan a la medida. Respondemos en menos de 24 horas.
                </p>
            </div>
        </header>

        <div class="contacto-grid">
            <div class="contacto-info">
                <h2>¿Por qué agendar una llamada?</h2>
                <ul>
                    <li>
                        <span class="title">Discovery express</span>
                        <span class="copy">Validamos objetivos, alcance y tiempos en una sola sesión.</span>
                    </li>
                    <li>
                        <span class="title">Equipo senior</span>
                        <span class="copy">Especialistas en productos digitales, integraciones y automatización.</span>
                    </li>
                    <li>
                        <span class="title">Presupuesto claro</span>
                        <span class="copy">Entregamos estimaciones y roadmap con hitos verificables.</span>
                    </li>
                </ul>

                <div class="cta-card">
                    <div class="cta-content">
                        <p class="cta-title">¿Prefieres WhatsApp?</p>
                        <a
                            href="https://wa.me/51939836897?text=Hola%20Cognicode,%20quiero%20hablar%20sobre%20un%20nuevo%20proyecto"
                            target="_blank"
                            class="cta-button"
                        >
                            Escribir ahora
                        </a>
                    </div>
                    <div class="cta-illustration" aria-hidden="true">
                        <img
                            src="https://blush.design/api/download?shareUri=JVuPyaXVpjBVkzPX&c=Bottom_0%7E393f82_Hair_0%7E2c1b18_Skin_0%7Edcae92_Top_0%7Eff4133&w=800&h=800&fm=png"
                            alt="Ilustración decorativa de persona sentada"
                            loading="lazy"
                        />
                    </div>
                </div>
            </div>

            <form class="contact-form" method="post">
                <div class="form-row">
                    <label for="nombre" class="field">
                        <span>Nombre completo</span>
                        <input id="nombre" name="nombre" type="text" placeholder="María López" required />
                    </label>

                    <label for="correo" class="field">
                        <span>Correo de contacto</span>
                        <input id="correo" name="correo" type="email" placeholder="hola@empresa.com" required />
                    </label>
                </div>

                <label for="tipoProyecto" class="field">
                    <span>¿Qué tipo de proyecto necesitas?</span>
                    <select id="tipoProyecto" name="tipoProyecto" required>
                        <option value="" disabled selected>Selecciona una opción</option>
                        <option value="web">Sitio web / Landing page</option>
                        <option value="ecommerce">E-commerce</option>
                        <option value="app">Aplicación web o móvil</option>
                        <option value="erp">ERP / Gestión interna</option>
                        <option value="otro">Otro tipo de solución</option>
                    </select>
                </label>

                <div class="field">
                    <span>¿Eres persona natural o empresa?</span>
                    <div class="toggle-group">
                        <label class="toggle">
                            <input type="radio" name="tipoCliente" value="persona" required />
                            <span>Persona</span>
                        </label>
                        <label class="toggle">
                            <input type="radio" name="tipoCliente" value="empresa" />
                            <span>Empresa</span>
                        </label>
                    </div>
                </div>

                <div class="form-row">
                    <label for="timeline" class="field">
                        <span>¿Para cuándo te gustaría tener el sistema listo?</span>
                        <select id="timeline" name="timeline" required>
                            <option value="" disabled selected>Selecciona una opción</option>
                            <option value="1-2">1 - 2 meses</option>
                            <option value="3-4">3 - 4 meses</option>
                            <option value="5-6">5 - 6 meses</option>
                            <option value="flexible">Cuando esté listo, soy flexible</option>
                        </select>
                    </label>

                    <label for="presupuesto" class="field">
                        <span>Presupuesto disponible</span>
                        <select id="presupuesto" name="presupuesto" required>
                            <option value="" disabled selected>Selecciona un rango</option>
                            <option value="2000">Hasta S/ 2,000</option>
                            <option value="5000">S/ 2,000 - S/ 5,000</option>
                            <option value="10000">S/ 5,000 - S/ 10,000</option>
                            <option value="10000+">Más de S/ 10,000</option>
                        </select>
                    </label>
                </div>

                <label for="tipoEmpresa" class="field">
                    <span>¿Qué tipo de empresa/emprendimiento tienes?</span>
                    <select id="tipoEmpresa" name="tipoEmpresa" required>
                        <option value="" disabled selected>Selecciona el sector</option>
                        <option value="retail">Retail / E-commerce</option>
                        <option value="servicios">Servicios profesionales</option>
                        <option value="manufactura">Manufactura / Logística</option>
                        <option value="salud">Salud / Wellness</option>
                        <option value="educacion">Educación</option>
                    </select>
                </label>

                <button class="btn-enviar" type="submit">Enviar solicitud</button>
            </form>
        </div>
    </section>
</Layout>

<style>
    :global(body) {
        background: linear-gradient(180deg, #f9fafc 0%, #f2f5fb 100%);
    }

	.contacto-wrapper {
		padding: 60px 20px 100px;
		max-width: 1200px;
		margin: 0 auto;
		color: #1a1a1a;
		font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
	}

	.contacto-hero {
		text-align: center;
		max-width: 900px;
		margin: -90px auto 48px;
		position: relative;
		padding-top: 210px;
	}

	.hero-wave {
		position: absolute;
		left: 50%;
		top: 0px;
		transform: translateX(-50%);
		width: 150%;
		pointer-events: none;
		opacity: 0.95;
		z-index: 0;
	}

	.hero-wave svg {
		width: 100%;
		height: 100%;
		display: block;
	}

	.hero-card {
		position: relative;
		z-index: 1;
		border-radius: 26px;
		padding: 46px 42px;
		box-shadow: 0 30px 65px rgba(14, 39, 79, 0.16);
		border: 1px solid rgba(10, 132, 255, 0.1);
	}

	.hero-pill {
		display: inline-flex;
		padding: 8px 16px;
		border-radius: 999px;
		background: rgba(10, 132, 255, 0.12);
		color: #0a84ff;
		font-size: 0.85rem;
		font-weight: 600;
		letter-spacing: 0.4px;
		text-transform: uppercase;
		margin-bottom: 16px;
	}

	.contacto-hero h1 {
		font-size: clamp(2.5rem, 5vw, 3.2rem);
		margin: 0 0 20px;
		font-weight: 800;
		letter-spacing: -1px;
	}

	.hero-copy {
		margin: 0;
		font-size: 1.05rem;
		color: #5e6373;
		line-height: 1.6;
	}

	.contacto-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
		gap: 32px;
	}

	.contacto-info {
		background: #ffffff;
		border-radius: 24px;
		padding: 40px;
		box-shadow: 0 20px 60px rgba(23, 51, 102, 0.12);
	}

	.contacto-info h2 {
		margin: 0 0 24px;
		font-size: 1.5rem;
	}

	.contacto-info ul {
		list-style: none;
		margin: 0 0 32px;
		padding: 0;
		display: flex;
		flex-direction: column;
		gap: 18px;
	}

	.contacto-info li {
		background: #f6f8fb;
		border-radius: 16px;
		padding: 16px 18px;
	}

	.contacto-info .title {
		display: block;
		font-weight: 700;
		margin-bottom: 6px;
		color: #0a84ff;
	}

	.contacto-info .copy {
		color: #5e6373;
		font-size: 0.95rem;
	}

	.cta-card {
		position: relative;
		overflow: visible;
		padding: 24px 24px 32px;
		border-radius: 18px;
		background: linear-gradient(135deg, rgba(10, 132, 255, 0.08), rgba(30, 124, 232, 0.08));
		display: flex;
		flex-direction: column;
		gap: 10px;
	}

	.cta-content {
		display: flex;
		flex-direction: column;
		align-items: flex-end;
		text-align: right;
		gap: 12px;
	}

	.cta-title {
		margin: 0 0 10px;
		font-weight: 600;
		color: #1a1a1a;
	}

	.cta-button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 12px 26px;
		border-radius: 12px;
		background: #0fbe73;
		color: #ffffff;
		font-weight: 700;
		text-decoration: none;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
		box-shadow: 0 15px 30px rgba(16, 190, 115, 0.35);
		position: relative;
		z-index: 2;
	}

	.cta-button:hover {
		transform: translateY(-2px);
		box-shadow: 0 20px 32px rgba(16, 190, 115, 0.45);
	}

	.contact-form {
		background: #ffffff;
		border-radius: 24px;
		padding: 40px;
		box-shadow: 0 24px 80px rgba(10, 24, 59, 0.12);
		display: flex;
		flex-direction: column;
		gap: 24px;
		position: relative;
		overflow: visible;
	}

	.form-row {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 20px;
	}

	.field {
		display: flex;
		flex-direction: column;
		gap: 10px;
		font-size: 0.95rem;
		color: #111827;
		font-weight: 600;
	}

	.field input,
	.field select,
	.field textarea {
		border: 1.5px solid rgba(10, 132, 255, 0.2);
		border-radius: 12px;
		padding: 14px 16px;
		font-size: 1rem;
		font-family: inherit;
		background: #fdfdff;
		transition: border-color 0.2s ease, box-shadow 0.2s ease;
	}

	.field input:focus,
	.field select:focus,
	.field textarea:focus {
		outline: none;
		border-color: #0a84ff;
		box-shadow: 0 0 0 3px rgba(10, 132, 255, 0.15);
	}

	.toggle-group {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
		gap: 12px;
	}

	.toggle {
		position: relative;
		border: 1.5px solid rgba(10, 132, 255, 0.25);
		border-radius: 12px;
		padding: 12px 14px;
		cursor: pointer;
		transition: all 0.2s ease;
		font-weight: 600;
		color: #5e6373;
		background: #f9fbff;
	}

	.toggle input {
		opacity: 0;
		position: absolute;
		inset: 0;
		cursor: pointer;
	}

	.toggle:has(input:checked) {
		border-color: #0a84ff;
		background: rgba(10, 132, 255, 0.08);
		color: #0a3bff;
		box-shadow: 0 10px 24px rgba(7, 78, 224, 0.15);
	}

	textarea {
		resize: vertical;
	}

	.btn-enviar {
		border: none;
		padding: 16px 24px;
		border-radius: 12px;
		font-size: 1rem;
		font-weight: 700;
		background: linear-gradient(135deg, #0a84ff, #1e7ce8);
		color: white;
		cursor: pointer;
		box-shadow: 0 16px 34px rgba(10, 132, 255, 0.25);
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}

	.btn-enviar:hover {
		transform: translateY(-1px);
		box-shadow: 0 20px 40px rgba(10, 132, 255, 0.35);
	}

	.cta-illustration {
		position: absolute;
		bottom: -20px;
		left: -10px;
		pointer-events: none;
	}

	.cta-illustration img {
		width: 180px;
		height: auto;
		display: block;
		filter: drop-shadow(0 18px 32px rgba(199, 206, 221, 0.18));
		z-index: 3;
	}

	@media (max-width: 640px) {
		.contacto-wrapper {
			padding: 100px 16px 60px;
		}

		.contacto-info,
		.contact-form {
			padding: 28px;
		}

		.cta-card {
			padding-bottom: 24px;
		}

		.cta-content {
			align-items: center;
			text-align: center;
		}

		.cta-illustration {
			position: static;
			margin-top: 12px;
		}

		.cta-illustration img {
			width: 140px;
		}
	}
</style>
